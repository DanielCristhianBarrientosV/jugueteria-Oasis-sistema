# Documentación de .gitignore

Este documento explica qué archivos están excluidos del control de versiones y por qué.

## 📋 Archivos Ignorados

### 🤖 Archivos de Asistentes de IA
Estos archivos contienen instrucciones específicas para asistentes de IA y son locales a cada desarrollador:

- `WARP.md` - Instrucciones para Warp AI
- `CLAUDE.md` - Instrucciones para Claude AI
- `.cursorrules` - Reglas para Cursor IDE
- `.cursor/` - Directorio de configuración de Cursor
- `.github/copilot-instructions.md` - Instrucciones para GitHub Copilot

**Razón:** Cada desarrollador puede tener sus propias preferencias de configuración de IA.

### 🧪 Reportes de Pruebas
Archivos generados durante el desarrollo y testing:

- `TESTING_REPORT.md` - Reportes de pruebas de desarrollo
- `TEST_*.md` - Cualquier archivo de test temporal

**Razón:** Son reportes temporales de desarrollo local, no parte del código fuente.

### 🐳 Scripts de Desarrollo Local
Scripts y configuraciones específicas del entorno local:

- `start-db.sh` - Script para iniciar la base de datos local
- `docker-compose.yml` - Configuración de Docker local
- `docker-compose.*.yml` - Variantes de docker-compose

**Razón:** Cada desarrollador puede tener su propio setup de Docker o preferir otras formas de correr la BD.

### 📝 Logs
Archivos de registro generados durante el desarrollo:

- `*.log` - Cualquier archivo de log
- `logs/` - Directorio de logs
- `*.log.*` - Logs con extensiones adicionales

**Razón:** Los logs son específicos de cada ejecución y no deben formar parte del repositorio.

### 🗂️ Archivos Temporales
Archivos temporales creados durante el desarrollo:

- `*.tmp` - Archivos temporales
- `*.temp` - Archivos temporales alternativos
- `/tmp/` - Directorio temporal

**Razón:** Son archivos efímeros sin valor para el control de versiones.

## 🔒 Archivos Sensibles (Ya incluidos)

Estos archivos ya estaban en el `.gitignore` original:

- `.env*` - Variables de entorno (pueden contener secretos)
- `*.pem` - Certificados y claves privadas
- `/node_modules` - Dependencias instaladas
- `/.next/` - Build de Next.js
- `*.tsbuildinfo` - Caché de TypeScript

## ⚠️ Importante

Si necesitas compartir alguno de estos archivos con el equipo:

1. **Configuraciones de Docker**: Crear ejemplos como `docker-compose.example.yml`
2. **Scripts de desarrollo**: Agregar a la documentación del README.md
3. **Configuraciones de IA**: Crear una guía en el README o Wiki

## 🔄 Comandos Útiles

Verificar si un archivo está siendo ignorado:
```bash
git check-ignore -v nombre-del-archivo
```

Ver todos los archivos ignorados en el proyecto:
```bash
git status --ignored
```

Ver solo archivos no trackeados:
```bash
git status --porcelain | grep "^??"
```

## 📚 Recursos

- [Documentación de .gitignore](https://git-scm.com/docs/gitignore)
- [Plantillas de .gitignore](https://github.com/github/gitignore)
