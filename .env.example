# Ejemplo de configuración de variables de entorno
# Copiar este archivo a .env y ajustar los valores según sea necesario

# Base de datos SQL Server
DATABASE_URL="sqlserver://localhost:1434;database=Oasis;user=SA;password=MSSQL@oasisdb;trustServerCertificate=true;"

# NextAuth (aunque usamos JWT personalizado, estas variables pueden ser útiles)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="genera-una-clave-secreta-con-openssl-rand-base64-32"

# JWT Secret (usado para firmar los tokens JWT)
# Generar con: openssl rand -base64 32
JWT_SECRET="tu-jwt-secret-muy-seguro-cambialo-en-produccion"

# Node Environment
NODE_ENV="development"

# Opcionales: Servicios externos
# CLOUDINARY_URL=""
# FIREBASE_API_KEY=""
# FIREBASE_AUTH_DOMAIN=""
# FIREBASE_PROJECT_ID=""
